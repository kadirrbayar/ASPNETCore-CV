@model EntityLayer.Concrete.Service
@{
    ViewData["Title"] = "ServiceAdd";
    Layout = "~/Views/Shared/_AdminPanelLayout.cshtml";
}

<div class="col-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Hizmet Ekleme Paneli</h4>
            @using (Html.BeginForm("ServiceAdd", "Service", FormMethod.Post))
            {
                <div class="form-group">
                    <label>Deneyim Başlığı</label>
                    @Html.TextBoxFor(x => x.Title, new {@class="form-control", @required="required"})
                </div>
                <div class="form-group">
                    <label>Image Url</label>
                    @Html.TextBoxFor(x => x.ImageURL, new {@class="form-control", @required="required"})
                </div>
                <button type="submit" class="btn btn-primary mr-2">Güncelle</button>
                <a href="/adminpanel/index" class="btn btn-dark">İptal</a>
            }
        </div>
    </div>
</div>


