@model EntityLayer.Concrete.WriterMessage

@{
    ViewData["Title"] = "SenderMessageDetails";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Mesaj Detayları</h4>
                @Html.HiddenFor(x=> x.MessageId)
                <div class="form-group">
                    <div class="form-group">
                        <h4>Konu:</h4>
                        <label>@Model.Subject</label>
                    </div>
                    <div class="form-group">
                        <h4>Gönderen Mail:</h4>
                        <label>@Model.Sender</label>
                    </div>
                    <div class="form-group">
                        <h4>Gönderen İsim:</h4>
                        <label>@Model.SenderName</label>
                    </div>
                    <div class="form-group">
                        <h4>Mesaj İçeriği:</h4>
                        <label>@Model.Content</label>
                    </div>
                </div>
                <a href="/Writer/Message/ReceiverMessage" class="btn btn-dark">İptal</a>
            </div>
        </div>
    </div>
</div>

